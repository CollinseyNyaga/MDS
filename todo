Add an element to a universal container instance, as a string. 
Print out the elements of the final universal container. 

finesse the compilation process of the test by creating the bash scripts better
